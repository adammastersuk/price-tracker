export function hasCompetitorData(competitorData: unknown): boolean;
export function shouldShowCompetitorInlineNote(label: string, competitorData: unknown): boolean;
export function checkedAtPill(checkedAt: string | null): { label: string; tone: string } | null;
